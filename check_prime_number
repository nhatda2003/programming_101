#Remember to add '+1' to int(x**(1/2)), if not it will go wrong
def isPrime(x):
        
        for i in range(2,int(x**(1/2))+1):
            if x%i == 0:
                return False
        return True
